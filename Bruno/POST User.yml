info:
  name: POST User
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:5001/users
  headers:
    - name: API-KEY
      value: "{{API-KEY}}"
  body:
    type: json
    data: |-
      {
            "FirstName":"Gustav",
            "LastName":"Rasmussen",
            "Email":"Gustav@gmail.com", 
            "Password":"270303" 
        }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
