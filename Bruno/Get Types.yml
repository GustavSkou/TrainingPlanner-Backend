info:
  name: Get Types
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:5001/types
  headers:
    - name: API-KEY
      value: "{{API-KEY}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
